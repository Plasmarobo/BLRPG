.row
  .col-sm-12.vh_hint
    Name
- armors.each_with_index do |armor, index|
  .row{class: "armor_row #{listRowClass(index)}", instance_id: "#{armor.id}"}
    = render partial: 'armor/instance_row', locals: {armor: armor}